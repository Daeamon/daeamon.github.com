//- This is a response to a delete action
- if(app.status == "success")
	h3 Done!
	p The app is now deleted
- else 
	h3 Error!
	p There was a problem and we couldn't delete your app. 
	//- p #{app.status}
	p Please try again later
