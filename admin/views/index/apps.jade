section#apps
	- each app in applist
		- var appStatus = app.running == "true";
		section(class="app-listing r10")
			hgroup
				h3(class=(appStatus) ? "app-running" : "app-stopped")
					a(href="/apps/"+app.name, title="click for more details...")#{app.name}
				ul.properties
					li ID: #{app.pid}
					li Port: #{app.port}
			div.git
				p(class="button yellowgrad r5", title="copy to clipboard")
					img(src="/static/img/icons/git-copy.png", title="copy to clipboard", height="22")
				input(type="text", value=app.gitrepo, readonly="readonly")